# Local Cleanup Notes

Cleanup manifests and pruning notes for generated performance data are local
artifacts. Store them under:

```text
validation/performance/local_results/cleanup/
```

The public repository keeps this README only so future sessions know where those
notes belong.
