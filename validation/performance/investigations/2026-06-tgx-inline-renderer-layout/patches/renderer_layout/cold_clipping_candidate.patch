diff --git a/src/Renderer3D.inl b/src/Renderer3D.inl
index 1b1f2c0..f2244b1 100644
--- a/src/Renderer3D.inl
+++ b/src/Renderer3D.inl
@@ -738,7 +738,7 @@ namespace tgx
 
 
 
-        template<typename color_t, Shader LOADED_SHADERS, typename ZBUFFER_t, int MAX_DIRECTIONAL_LIGHTS, int MAX_SPOT_LIGHTS> TGX_NOINLINE
+        template<typename color_t, Shader LOADED_SHADERS, typename ZBUFFER_t, int MAX_DIRECTIONAL_LIGHTS, int MAX_SPOT_LIGHTS> TGX_NOINLINE TGX_RP2350_CLIPPING_NOINLINE
         void Renderer3D<color_t, LOADED_SHADERS, ZBUFFER_t, MAX_DIRECTIONAL_LIGHTS, MAX_SPOT_LIGHTS>::_drawTriangleClipped(const int RASTER_TYPE,
             const fVec4* Q0, const fVec4* Q1, const fVec4* Q2,
             const fVec3* N0, const fVec3* N1, const fVec3* N2,
@@ -803,7 +803,7 @@ namespace tgx
             }
 
 
-        template<typename color_t, Shader LOADED_SHADERS, typename ZBUFFER_t, int MAX_DIRECTIONAL_LIGHTS, int MAX_SPOT_LIGHTS> TGX_NOINLINE
+        template<typename color_t, Shader LOADED_SHADERS, typename ZBUFFER_t, int MAX_DIRECTIONAL_LIGHTS, int MAX_SPOT_LIGHTS> TGX_NOINLINE TGX_RP2350_CLIPPING_NOINLINE
         void Renderer3D<color_t, LOADED_SHADERS, ZBUFFER_t, MAX_DIRECTIONAL_LIGHTS, MAX_SPOT_LIGHTS>::_drawTriangleClippedSub(const int RASTER_TYPE, const int plane, const RasterizerVec4& P1, const RasterizerVec4& P2, const RasterizerVec4& P3)
             {
 
diff --git a/src/tgx_config.h b/src/tgx_config.h
index 93dcbd9..f9c575b 100644
--- a/src/tgx_config.h
+++ b/src/tgx_config.h
@@ -286,6 +286,15 @@
 #endif
 
 
+#ifndef TGX_RP2350_CLIPPING_NOINLINE
+    #if (defined(ARDUINO_ARCH_RP2350) || defined(PICO_RP2350) || defined(TARGET_RP2350)) && defined(__GNUC__)
+        #define TGX_RP2350_CLIPPING_NOINLINE __attribute__((noinline))
+    #else
+        #define TGX_RP2350_CLIPPING_NOINLINE
+    #endif
+#endif
+
+
 
 
 
