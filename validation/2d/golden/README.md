# 2D Image Goldens

Large 2D BMP golden images are kept locally under:

```text
validation/local_results/golden/2d/cpu
```

They are intentionally ignored by Git. The tracked CSV/hash baselines remain in
`validation/2d/baselines/`.
