# CPU Runner

`bench3d_cpu_main.cpp` is the native desktop entry point for the modular 3D
benchmark suite.

It can run the same modules as the MCU builds, write the same telemetry format,
and optionally display animated validation frames with CImg or save 4x5 mosaics
for visual review. Use the Python runner from `tools/` for normal invocation.
