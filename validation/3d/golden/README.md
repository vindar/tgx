# 3D Image Goldens

Large 3D BMP golden images are kept locally under:

```text
validation/local_results/golden/3d/cpu
```

They are intentionally ignored by Git. The tracked CSV/signature baseline
remains in `validation/3d/baselines/`.
